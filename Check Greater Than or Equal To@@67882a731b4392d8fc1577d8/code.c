#include <stdio.h>
int main {
    int x,y;
    scanf("%d",&x);
    scanf("%d",&y);
    if{(x>=y)&&(x==y)}
    {
        printf("True");
    }
    else
    {
        printf("False");
    }
}